- flash_mapping = {'success' => 'success', 'error' => 'danger', 'persistent' => 'success -persistent'}
.alerts(up-hungry)
  - flash.each do |level, message|
    %div{class: "alert alert-#{flash_mapping.fetch(level)}"}
      = message
      %button(type='button' class='close' aria-label='Close')
        %span(aria-hidden) ×
